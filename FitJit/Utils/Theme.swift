import SwiftUI

struct Theme {
    // Main colors
    static let background = Color.white
    static let primary = Color(UIColor.systemGray)
    static let secondary = Color.black
    static let accent = Color(UIColor.systemGray2)
    
    // Text colors
    static let text = Color.black
    static let buttonText = Color.white
    
    // UI Element colors
    static let sliderTrack = Color(UIColor.systemGray5)
    static let divider = Color(UIColor.systemGray4)
}